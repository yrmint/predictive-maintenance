[project]
name = "predictive-maintenance"
version = "0.1.0"
description = ""
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
    "sensor",
]

[dependency-groups]
dev = [
    "ruff>=0.12.0"
]